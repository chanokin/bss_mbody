from __future__ import (print_function,
                        # unicode_literals,
                        division)
from builtins import open, range, dict

BLACKLISTS = {33: {row: [] for row in range(16)}}
